using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Section1Complete : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello ProgrammerLand");
    }

    // Update is called once per frame
    void Update()
    {
        // what happens if we put it here?
    }
}
